---
declared-services:
  Conversation-IC:
    label: conversation
    plan: free
applications:
- name: c-chat
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - Conversation
  env:
    NPM_CONFIG_PRODUCTION: false
